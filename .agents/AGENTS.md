# Security Guidelines

- Never stage, commit, or push `.env` files, API keys, tokens, or sensitive credentials to GitHub or any remote repository.
- Always check git diff and git status to verify no sensitive data or environment secrets are being committed.
